<?php
// Initialize the variable
$counter = 1;

// The while loop will run as long as $counter is less than or equal to 5
while ($counter <= 5) {
    // Print the current value of $counter
    echo "Counter is at: $counter \n";

    // Increment $counter by 1
    $counter++;
}

